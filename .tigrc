bind main R !?git rebase -i %(commit)^
bind status r !git rebase --continue
bind status c !git commit --no-verify
bind status A !?git commit --amend --no-edit --no-verify
bind status p !?git push origin %(branch)
bind status P !?git push origin %(branch) -f
bind status D !rm %(file)
